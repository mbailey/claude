---
description: Review git repositories and commit changes using git-hygiene skill
---

Load the git-hygiene skill to review uncommitted changes, check sync status with remotes, and efficiently commit and push changes.

The user wants to review git repositories in: {{arg "directory" "~/Code/github.com/mbailey/"}}

Use the git-hygiene skill to:
1. Check the status of repositories in the specified directory
2. Show uncommitted changes across repositories
3. Help commit and push changes as needed
4. Ensure work is synced between machines

After loading the skill, follow its instructions for token-efficient git workflow management with progressive disclosure.
