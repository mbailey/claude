// Boards of Canada Inspired Soundscape
// Nostalgic, warm, slightly degraded ambient textures

// Layer 1: Warm analog-style bass
// Deep foundation with gentle filtering
$: note("<[c2,g2]@8 [bb1,f2]@8>")
  .sound("sawtooth")
  .lpf(sine.range(150, 250).slow(20))  // slow filter movement
  .room(0.9)
  .gain(0.28)
  .slow(8)

// Layer 2: Nostalgic pad chords
// Slightly detuned for analog character
$: note("<[g3,b3,d4] [a3,c4,e4] [f3,a3,c4]>")
  .sound("triangle")
  .lpf(1000)
  .room(0.75)
  .delay(0.35)
  .gain(0.42)
  .slow(4)
  .jux(x => x.speed(0.995))  // subtle detune in right channel

// Layer 3: Degraded texture layer
// Euclidean pattern with slightly random speed for imperfection
$: s("space(5,13)")
  .speed("<0.65 0.7 0.68 0.72>")
  .room(0.82)
  .lpf(750)
  .gain(0.32)
  .slow(3)

// Layer 4: Childlike melody fragments
// Sparse, simple notes with heavy processing
$: note("~ ~ c5 ~ ~ e5 ~ ~ ~ g5 ~ ~ ~ ~ ~ ~")
  .sound("sine")
  .room(0.92)
  .delay(0.65)
  .gain(0.35)
  .slow(6)

// Layer 5: Very occasional low bell
// Rare accent for depth
$: note("~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ c3 ~")
  .sound("triangle")
  .room(0.95)
  .lpf(400)
  .gain(0.25)
  .slow(12)

// Characteristics of BoC-style ambient:
// - Warm, slightly lo-fi quality (achieved through low-pass filtering)
// - Nostalgic, childlike simplicity (sparse melodies, simple intervals)
// - Analog imperfection (subtle pitch drift with speed variations)
// - Heavy reverb and delay (spatial depth)
// - Slow evolution (long .slow() values)
// - Modal harmony (avoiding complex jazz chords)
